# Keep the ASGI test stack on a compatible release line.  The unbounded
# FastAPI/Starlette requirements previously allowed Starlette 1.x to be
# installed alongside httpx 0.28, which can block TestClient forever.
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]>=0.28.0,<0.35.0
websockets>=12.0,<14.0
pydantic>=2.6.0,<3.0.0
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx==0.28.1
anyio==4.8.0
httpcore==1.0.7
